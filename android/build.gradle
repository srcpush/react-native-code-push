apply plugin: 'com.android.library'

android {
    namespace "com.microsoft.codepush.react"
    compileSdkVersion 33

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }
}

repositories {
    mavenLocal()
    mavenCentral()
}